#pragma once
#ifndef SORT_ARRAY_BY_PARITY_H
#define SORT_ARRAY_BY_PARITY_H

#include <vector>

using namespace std;

class Solution {
public:
	vector<int> sortArrayByParity(vector<int>& nums) {
		vector<int> evens;
		vector<int> odds;
	}
};

#endif